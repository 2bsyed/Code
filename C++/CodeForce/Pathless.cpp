#include<bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        int s;
        cin >> n >> s;
        vector<int> v(n);
        for (int i : v) {
            cin >> v[i];
        }
    }